/**
 * Test stub for `expo-crypto`.
 *
 * Only `randomUUID` is used by the code under test, and Node's own implementation
 * produces the same UUID v4 format.
 */
import { randomUUID } from 'node:crypto';

export { randomUUID };
